apiVersion: apps/v1
kind: Deployment

spec:
  replicas: 1
  selector:
    
  template:
      metadata:
        name: v
      spec:
        containers:
        - name: voting-app
          image: dockersamples/examplevotingapp_vote
          ports:
            - containerPort: 109
